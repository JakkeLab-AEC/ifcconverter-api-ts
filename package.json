{
  "name": "ifcconverter-api-ts",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc && npm pack --pack-destination ./packed/"
  },
  "dependencies": {
    "typescript": "^5.7.3"
  },
  "keywords": ["ifc", "ifcConverter", "typescript"],
  "author": "JakkeLab",
  "license": "MIT",
  "description": "A TypeScript-based API for creating data to use IfcConverter",
  "files":[
    "dist"
  ]
}
